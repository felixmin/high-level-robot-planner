# Todos from today
- add viz to stage 3
    - the standard viz from our toolbox and others
- fix the issue for stage 3 with only 10% of data
    - generally we should have "clean" batches otherwise the gradients are too noisy for batches with e.g. 3 samples 
    - check latent_losses_strategy.md
- add shared core for stage1 decoders
- fix dirty lerobot
- setup torchcodec backend
- test stage12 and stage3 dockerfiles

- check the stage 2 and 3 defaults and compare them... e.g. network weight initialization, image size and normalization etc -> can we be sure that stage 2 initialized weights dont start worse?
    -> We could compare by initializing stage2 without training and directly using this artifact for stage 3 -> it must perform the same as stage3 initialized from scratch




- check normalization
    - is it happening across stages? particularly stage2 ?
    - is normalization similar for stage 2 and 3?
- check action padding
- check image sizes -> stage 2 vs stage 3 differences?
- check image keys
    - is stage 1 using the right key?, what happens if no key selected?
    - normalize datasets to a common naming pattern with a per dataset config
- check types and contracts in lerobot repo... can we use the same? 
    - check "datasets_to_policy_features" 707

- think about what role our repo takes... it is like a custom train script in lerobot??
    - if we only work with lerobot datasets we can take their dataloader?? is it fast enough?
    - 

- check the interpolate function in modeling_hlrp_... lerobot policy code for correctness and what it does
- check all commands that submitjob adds when submitting script 6_... is it a proper lerobot config with the right vars? is anything set unexpected or wrong or different from the normal lerobot defaults?
    - e.g. dataset root
