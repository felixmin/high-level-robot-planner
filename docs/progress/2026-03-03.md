# 2026-03-03

## Implementation continuation

### Native Stage-1 batch path

Additional implementation work completed after the initial LeRobot-v3 backend integration:

- Stage 1 callbacks and dataset-usage logging now accept native `Stage1Batch` objects instead of only legacy dict batches.
- `config/experiment/stage1_laq_lerobot_v3_smoke.yaml` now uses `data.output_format=stage1`.
- Added practical local configs:
  - `config/experiment/stage1_laq_lerobot_v3_local.yaml`
  - `config/experiment/stage1_laq_lerobot_v3_mix_local.yaml`
  - `config/experiment/stage2_smol_flow_lerobot_v3_local.yaml`
  - `config/experiment/stage2_smol_actions_lerobot_v3_smoke.yaml`

Found and fixed one real runtime issue:

- `LAQTask.transfer_batch_to_device(...)` previously delegated frozen `Stage1Batch` dataclasses to Lightning's generic device mover and failed.
- The fix is to handle `Stage1Batch` before `super()`, move tensors explicitly, and convert uint8 image streams to float32 `[0,1]`.

Regression coverage added:

- `tests/test_vla_module_foundation_batch.py`
  - `test_laq_task_transfer_batch_to_device_accepts_stage1_batch`
- `tests/test_dataset_usage_logger_callback.py`
  - `test_dataset_usage_logger_accepts_stage1_batch_meta`
  - `test_dataset_usage_logger_accepts_foundation_batch_meta`
- `tests/laq/test_validation.py`
  - `test_train_preview_buffer_accepts_stage1_batch`
  - `test_validation_callback_accepts_stage1_batch`
- `tests/test_lerobot_v3_datamodule.py`
  - `test_lerobot_v3_datamodule_stage1_output_returns_stage1_batch`

Legacy cleanup completed later in the same session:

- removed the remaining `stage1_legacy` LeRobot-v3 compatibility branch from:
  - `packages/common/lerobot_v3_adapters.py`
  - `packages/common/lerobot_v3_data.py`
- removed the dead `legacy_stage1_camera_role` config knob from:
  - `config/data/lerobot_v3.yaml`
- switched the LeRobot-v3 dataloader benchmark to native `stage1` output as well
- kept one regression test that intentionally requests `output_format=\"stage1_legacy\"` and now asserts a clear failure

Local native Stage-1 run:

- command:
  - `HF_LEROBOT_HOME=/mnt/data/workspace/runs_root/cache/huggingface/lerobot CONDA_NO_PLUGINS=true conda run -n hlrp python scripts/2_train_laq.py experiment=stage1_laq_lerobot_v3_smoke logging.use_wandb=false logging.root_dir=/tmp/hlrp_root logging.runs_dir=/tmp/hlrp_runs precision=32-true`
- result:
  - succeeded against cached official `lerobot/nyu_rot_dataset`
  - checkpoint written to `/tmp/hlrp_runs/checkpoints/last.ckpt`

Operational note:

- local CPU execution required `precision=32-true` because bf16 backward failed in oneDNN.

### Stage-2 action-only path

After rechecking the code path, the only remaining practical blocker was that `scripts/4_train_foundation.py` still loaded a Stage-1 LAQ checkpoint unconditionally even for `model.training_mode=actions`.

This was tightened:

- `scripts/4_train_foundation.py`
  - now loads LAQ only for `codes`, `latent_flow`, and `multitask`
  - skips LAQ entirely for `actions`
  - uses `smolvla_core.latent.latent_vector_dim` when no LAQ provider exists
- `packages/foundation/vla_backend_module.py`
  - now treats `code_provider` as optional
  - fails fast with a clear `ValueError` only when a code-generating mode actually needs it

Regression coverage added:

- `tests/test_vla_module_foundation_batch.py`
  - `test_vla_module_actions_mode_does_not_require_code_provider`
  - `test_vla_module_latent_flow_requires_code_provider`

Local action-only Stage-2 smoke:

- command:
  - `HF_LEROBOT_HOME=/mnt/data/workspace/runs_root/cache/huggingface/lerobot CONDA_NO_PLUGINS=true conda run -n hlrp python scripts/4_train_foundation.py experiment=stage2_smol_actions_lerobot_v3_smoke logging.use_wandb=false logging.root_dir=/tmp/hlrp_root logging.runs_dir=/tmp/hlrp_runs precision=32-true`
- result:
  - succeeded without `model.laq.checkpoint`
  - reused cached official `lerobot/nyu_rot_dataset`
  - exported `/tmp/hlrp_runs/artifacts/smolvla_shared_stage2_artifact.pt`

Current practical state after this continuation:

- Stage 1 native `Stage1Batch` path works locally on a real official LeRobot v3 OXE dataset.
- Stage 2 action-only path works locally on a real official LeRobot v3 OXE dataset.
- The old raw-OXE parity task remains optional and is no longer blocking practical adoption of the LeRobot-v3-first path.

### Stage-2 latent-flow local workflow

One remaining real integration issue showed up when exercising the practical latent-flow config:

- Stage 2 used the LeRobot-v3-resized VLM images directly for online LAQ supervision.
- A Stage-1 LAQ checkpoint can expect a different image size than the Stage-2 VLM path.
- Concrete failure:
  - VLM images at `256x256`
  - LAQ checkpoint at `128x128`
  - previous result: `ValueError: Expected image size (128, 128), got (256, 256)`

Fix:

- `packages/foundation/vla_backend_module.py`
  - now resizes only the temporary online-LAQ video tensor to `code_provider.image_size`
  - keeps the Stage-2 `FoundationBatch.image_streams` unchanged for the VLM/backend path
- `packages/foundation/online_laq.py`
  - `LAQTaskCodeProvider` now exposes `image_size`

Regression coverage added:

- `tests/test_vla_module_foundation_batch.py`
  - `test_vla_module_resizes_online_laq_video_to_teacher_image_size`

Practical latent-flow validation:

- command:
  - `HF_LEROBOT_HOME=/mnt/data/workspace/runs_root/cache/huggingface/lerobot CONDA_NO_PLUGINS=true conda run -n hlrp python scripts/4_train_foundation.py experiment=stage2_smol_flow_lerobot_v3_local logging.use_wandb=false logging.root_dir=/tmp/hlrp_root_flow logging.runs_dir=/tmp/hlrp_runs_flow model.laq.checkpoint=/tmp/hlrp_runs/checkpoints/last.ckpt training.max_steps=2 training.validation.check_interval=2 training.validation.limit_batches=1 data.loader.batch_size=2 data.loader.num_workers=0 training.checkpoint.every_n_train_steps=1`
- result:
  - succeeded after the resize fix
  - exported `/tmp/hlrp_runs_flow/artifacts/smolvla_shared_stage2_artifact.pt`

Updated practical state:

- Stage 1 single-source LeRobot-v3 path works locally.
- Stage 1 mixed-source LeRobot-v3 path works locally.
- Stage 2 action-only LeRobot-v3 path works locally.
- Stage 2 latent-flow LeRobot-v3 path works locally against a real Stage-1 checkpoint.

### Stage-2 mixed-source validation

To cover the original multi-dataset requirement on the Stage-2 path as well, a dedicated tiny mixed action-smoke config was added:

- `config/experiment/stage2_smol_actions_lerobot_v3_mix_smoke.yaml`

Config intent:

- weighted two-source Stage-2 action-only smoke
- sources:
  - `lerobot/nyu_rot_dataset`
  - `lerobot/asu_table_top`
- uses the shared LeRobot-v3 mixer and merged normalization stats path

Practical validation:

- command:
  - `HF_LEROBOT_HOME=/mnt/data/workspace/runs_root/cache/huggingface/lerobot CONDA_NO_PLUGINS=true conda run -n hlrp python scripts/4_train_foundation.py experiment=stage2_smol_actions_lerobot_v3_mix_smoke logging.use_wandb=false logging.root_dir=/tmp/hlrp_root_action_mix logging.runs_dir=/tmp/hlrp_runs_action_mix precision=32-true`
- result:
  - succeeded
  - exported `/tmp/hlrp_runs_action_mix/artifacts/smolvla_shared_stage2_artifact.pt`
  - validation usage log reported:
    - `lerobot/asu_table_top=2`
    - `lerobot/nyu_rot_dataset=2`
  - this is the expected `50/50` split for the tiny validation batch

Updated current practical state:

- Stage 1 single-source LeRobot-v3 path works locally.
- Stage 1 mixed-source LeRobot-v3 path works locally.
- Stage 2 single-source action path works locally.
- Stage 2 mixed-source action path works locally.
- Stage 2 latent-flow path works locally against a real Stage-1 checkpoint.

### Cleanup pass after code review

I rechecked the LeRobot-v3 implementation against the repo preference for short, fail-fast research code and simplified a few implementation details.

Accepted cleanup changes:

- extracted shared tensor helpers into:
  - `packages/common/batch_utils.py`
- removed duplicated "pick primary camera + convert temporal frames to `[B,C,T,H,W]`" logic from:
  - `packages/laq/task.py`
  - `packages/laq/callbacks.py`
  - `packages/foundation/vla_backend_module.py`
  - `packages/foundation/online_laq.py`
- removed dead code from:
  - `LAQTask.transfer_batch_to_device(...)`
- replaced repetitive frozen-dataclass tensor device moves with one shared helper used by:
  - `LAQTask.transfer_batch_to_device(...)`
  - `VLATokenBackendLightningModule.transfer_batch_to_device(...)`
- removed the old float-image range heuristic in `packages/common/lerobot_v3_source.py`
  - source now accepts:
    - `uint8` images directly
    - floating images only under the explicit assumption that they are already in `[0,1]`
  - unsupported image dtypes fail immediately
- replaced the anonymous `type(...)` metadata wrapper with a real `SourceMetaView` dataclass
- removed redundant `source_name` from sample metadata and kept `dataset_name` as the canonical identity
- made unsupported stats merge modes fail fast:
  - `build_run_normalization_stats(..., weights_mode=...)` now raises `NotImplementedError` for anything except `explicit`
- fixed Stage-1 run-size logging in `scripts/2_train_laq.py`
  - `len(train_dataset)` is sample count, not batch count
- state pad masks now read the LeRobot-side `*_is_pad` key when present instead of always assuming no state padding

Review points intentionally not adopted:

- no `mix_block_size`
  - still intentionally omitted from the initial design to keep the mixer simple
- no extra per-dataset config abstraction yet
  - current experiment-config duplication is acceptable for the small number of practical LeRobot-v3 configs now in the repo
- no extra compatibility/error layer beyond a few targeted fail-fast checks
  - consistent with the repo's research-engineering style

Validation after the cleanup pass:

- targeted test command:
  - `PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 conda run -n hlrp pytest -q tests/test_vla_module_foundation_batch.py tests/test_lerobot_v3_source.py tests/test_lerobot_v3_stats.py tests/test_dataset_usage_logger_callback.py tests/test_lerobot_v3_datamodule.py tests/test_hydra_configs.py tests/laq/test_validation.py`
- result:
  - `102 passed`

Real local rerun after cleanup:

- command:
  - `HF_LEROBOT_HOME=/mnt/data/workspace/runs_root/cache/huggingface/lerobot CONDA_NO_PLUGINS=true conda run -n hlrp python scripts/2_train_laq.py experiment=stage1_laq_lerobot_v3_smoke logging.use_wandb=false logging.root_dir=/tmp/hlrp_root_cleanup logging.runs_dir=/tmp/hlrp_runs_cleanup precision=32-true`
- result:
  - succeeded
  - wrote `/tmp/hlrp_runs_cleanup/checkpoints/last.ckpt`

### Octo25 LeRobot-v3 mix setup

To match the existing custom-weighted Octo-style OXE composition, I added:

- `config/data/lerobot_v3_octo25.yaml`
- `scripts/download_lerobot_v3_sources.py`

The new data config mirrors the 25-source weights from `config/data/octo_dataset.yaml`, but maps them onto LeRobot-v3 repos.

Mapping policy used:

- prefer official `lerobot/...` repos when available,
- fall back to `FedorX8/..._lerobot` only for datasets missing from the official LeRobot collection.

Current Octo25 LeRobot-v3 mapping status:

- total sources: `25`
- official `lerobot/...`: `15`
- community fallback/provisional: `10`
- all mapped repos checked from the local metadata snapshot show `codebase_version: v3.0`

Important correction after rechecking the repo's own OXE registry:

- OXE `kuka` and `stanford_kuka_multimodal_dataset_converted_externally_to_rlds` are distinct datasets in `packages/common/adapters/oxe_shared.py`
- the first draft incorrectly mapped Octo `kuka` to official `lerobot/stanford_kuka_multimodal_dataset`
- that mapping was wrong
- after checking the current port metadata again, `kuka` is now excluded entirely from `lerobot_v3_octo25.yaml`
- the remaining `24` sources were renormalized to keep explicit weights summing to roughly `100`

Persistent cache status after finishing the current Octo24 download pass:

- cached community repos:
  - `FedorX8/fractal20220817_data_lerobot`
  - `FedorX8/bridge_v2_lerobot`
  - `FedorX8/bc_z_lerobot`
  - `FedorX8/language_table_lerobot`
  - `FedorX8/furniture_bench_dataset_lerobot`
  - `FedorX8/roboturk_lerobot`
  - `FedorX8/iamlab_cmu_pickup_insert_lerobot`
  - `FedorX8/viola_lerobot`
  - `FedorX8/berkeley_cable_routing_lerobot`
- cached official repos:
  - `lerobot/stanford_hydra_dataset`
  - `lerobot/taco_play`
  - `lerobot/utaustin_mutex`
  - `lerobot/austin_sailor_dataset`
  - `lerobot/toto`
  - `lerobot/austin_sirius_dataset`
  - `lerobot/berkeley_autolab_ur5`
  - `lerobot/berkeley_fanuc_manipulation`
  - `lerobot/nyu_franka_play_dataset`
  - `lerobot/jaco_play`
  - `lerobot/austin_buds_dataset`
  - `lerobot/cmu_stretch`
  - `lerobot/nyu_door_opening_surprising_effectiveness`
  - `lerobot/dlr_edan_shared_control`
  - `lerobot/ucsd_kitchen_dataset`

That completes the full current `24`-dataset Octo-style LeRobot-v3 mix defined in `config/data/lerobot_v3_octo25.yaml`.

Approximate total size from the per-repo `meta/info.json` size fields:

- total: about `12.6 GB`
- top 6 by weight:
  - `FedorX8/fractal20220817_data_lerobot`: about `0.6 GB`
  - `lerobot/stanford_kuka_multimodal_dataset`: about `0.6 GB`
  - `FedorX8/bridge_v2_lerobot`: about `0.3 GB`
  - `FedorX8/bc_z_lerobot`: about `0.3 GB`
  - `lerobot/stanford_hydra_dataset`: about `0.6 GB`
  - `FedorX8/language_table_lerobot`: about `0.3 GB`
- top 6 subtotal: about `2.7 GB`

Important caveat:

- those size numbers come from the cached `meta/info.json` snapshots and are coarse metadata values,
- they are good enough for planning order and rough storage expectations, but should be treated as approximate.

### Practical local mixed-source validation

The new non-smoke local Stage-1 mix config was also exercised directly:

- command:
  - `HF_LEROBOT_HOME=/mnt/data/workspace/runs_root/cache/huggingface/lerobot CONDA_NO_PLUGINS=true conda run -n hlrp python scripts/2_train_laq.py experiment=stage1_laq_lerobot_v3_mix_local logging.use_wandb=false logging.root_dir=/tmp/hlrp_root_mix logging.runs_dir=/tmp/hlrp_runs_mix precision=32-true training.max_steps=2 data.loader.batch_size=4 data.loader.num_workers=0 training.checkpoint.every_n_train_steps=1`
- result:
  - succeeded with a real weighted two-source mix:
    - `lerobot/nyu_rot_dataset`
    - `lerobot/asu_table_top`
  - fetched the missing official `asu_table_top` dataset files into the LeRobot cache on first use
  - also fetched the RAFT teacher checkpoint into `/tmp/hlrp_root_mix/cache/torch/...` because the current Stage-1 config keeps flow enabled

Practical implication:

- the “usable workflow” configs are no longer only config-composition artifacts,
- both the single-source and mixed-source LeRobot-v3 Stage-1 paths now have real local train runs behind them.

## Dataset unification exploration

Objective for today:

- decide whether HLRP should stay on raw OXE, build a mixed raw-OXE-plus-LeRobot stack, or move toward a LeRobot-v3-first dataset stack,
- extract the actual dataset contract required by the codebase,
- document a migration path that keeps Stage 1, Stage 2, and Stage 3 aligned.

## Repo findings

### Current active path

- `packages/common/data_factory.py` currently only supports `data.backend=oxe_local_indexed`.
- Stage 1 still consumes `frames + metadata`.
- Stage 2 already normalizes into `FoundationBatch`, but only after going through an OXE-specific batch path.
- Stage 3 is already LeRobot-native and converts LeRobot batches into `FoundationBatch`-compatible inputs.

### Practical implication

The real abstraction gap is below Stage 1 and below `FoundationBatch`.

The clean solution is:

1. one HLRP temporal-window dataset contract,
2. one HLRP weighted mixer,
3. format-specific source adapters under that contract.

## External dataset findings

### Collections

- `FedorX8/oxe-datasets-lerobot-v30` contains 67 datasets.
- `lerobot/open-x-embodiment` contains 56 datasets and is the better migration target.

Correction after collection-wide `meta/info.json` check:

- `lerobot/open-x-embodiment` is mixed-version, not all v3:
  - `50` repos at `v3.0`
  - `4` repos at `v2.0`
  - `2` repos at `v2.1`
- `FedorX8/oxe-datasets-lerobot-v30` checked out as `65` repos at `v3.0`

Known non-v3 repos in `lerobot/open-x-embodiment`:

- `lerobot/aloha_static_tape` (`v2.0`)
- `lerobot/aloha_static_fork_pick_up` (`v2.0`)
- `lerobot/aloha_static_thread_velcro` (`v2.0`)
- `lerobot/aloha_static_battery` (`v2.0`)
- `lerobot/usc_cloth_sim` (`v2.1`)
- `lerobot/imperialcollege_sawyer_wrist_cam` (`v2.1`)

Updated implication:

- the official collection should be treated as a candidate pool, not as a uniformly ready v3 set,
- HLRP should filter for `codebase_version == v3.0`,
- for datasets available in both places, official and mirror versions still need per-dataset validation because collection membership/version alone does not guarantee identical content.

### Dataset page caveat

The Hugging Face dataset page can be misleading because the README may embed stale metadata examples.

Concrete example:

- `lerobot/stanford_kuka_multimodal_dataset`
  - actual `meta/info.json`: `codebase_version: v3.0`
  - dataset repo layout: `meta/info.json`, `meta/stats.json`, `meta/tasks.parquet`, `data/...`, `videos/...`
  - but the README on the dataset page currently shows an embedded `v2.0` JSON snippet under the `meta/info.json` section

Conclusion:

- for version checks, trust the actual `meta/info.json` file in the repo, not the rendered README snippet on the dataset page.

### Official vs mirror probe

Checked files:

- `meta/info.json`
- `meta/stats.json`
- `data/chunk-000/file-000.parquet`

Results:

- `jaco_play`
  - official and mirror matched for the probed files.
- `taco_play`
  - official and mirror did not match.
  - official: `3603` episodes, `237798` frames, state shape `[7]`
  - mirror: `3242` episodes, `213972` frames, state shape `[8]`

Conclusion:

- the mirror collection is not stable enough to use as the main source of truth,
- prefer official `lerobot/*` OXE ports.

## Raw local OXE spot checks

Local raw samples:

- `jaco_play`
  - first instruction: `pick up the butter dairy`
  - cameras in raw sample: `image`, `image_wrist`
  - raw action keys: `world_vector`, `gripper_closedness_action`, `terminate_episode`
- `taco_play`
  - first instruction: `rotate the yellow block to the left`
  - cameras in raw sample: `rgb_static`, `rgb_gripper`
  - raw state key `robot_obs` has shape `(15,)`
  - raw action keys include `actions`

Important note:

- the LeRobot v3 ports are canonicalized representations, not literal field-for-field copies.
- for example:
  - official `lerobot/jaco_play` uses `observation.state` shape `8`
  - official `lerobot/taco_play` uses `observation.state` shape `7`

This means the migration question is about semantic fitness of the canonicalized state, not only loader correctness.

## Runtime LeRobot probes for Stage 2 needs

Used `LeRobotDataset(..., episodes=[0], delta_timestamps=...)` with:

- one camera queried at `[0, pair_offset_steps / fps]`,
- `observation.state` at `[0]`,
- `action` over a future chunk.

Results:

### `lerobot/jaco_play`

- returned keys include:
  - `observation.images.image`
  - `observation.images.image_is_pad`
  - `observation.images.image_wrist`
  - `observation.state`
  - `action`
  - `action_is_pad`
  - `task`
  - `episode_index`
  - `frame_index`
- shapes:
  - image pair `(2, 3, 224, 224)`
  - state `(1, 8)`
  - action chunk `(10, 7)`
- end-of-episode padding behaved correctly:
  - `action_is_pad`: first step valid, remaining future steps padded
  - image pair pad mask: current frame valid, future frame padded when out of range
- important adapter note:
  - LeRobot still returned an extra non-queried camera (`observation.images.image_wrist`) as a single-frame observation.
  - HLRP should therefore explicitly select configured camera keys and normalize temporal rank inside the source adapter.

### `lerobot/taco_play`

- returned keys include:
  - `observation.images.rgb_static`
  - `observation.images.rgb_static_is_pad`
  - `observation.state`
  - `action`
  - `action_is_pad`
  - `task`
  - `episode_index`
  - `frame_index`
- shapes:
  - image pair `(2, 3, 150, 200)`
  - state `(1, 7)`
  - action chunk `(10, 7)`
- end-of-episode padding behaved correctly.

Operational note:

- these runtime probes downloaded more than metadata because OXE video-backed LeRobot datasets pull the full chunk file for the selected episode.
- observed download sizes:
  - `jaco_play`: about `429 MB`
  - `taco_play`: about `708 MB`

## Local OXE count sanity checks

Local tar scan counts:

- `jaco_play`: `976` episodes across `2` tar files
- `taco_play`: `3242` episodes across `11` tar files

Interpretation:

- local raw OXE storage should not be treated as the source of truth for completeness,
- the local dump appears partial or older for at least some datasets,
- this further supports validating against official Hub datasets rather than local shard counts alone.

## Decision after today’s exploration

Preferred path:

1. move toward official LeRobot v3 datasets as the long-term dataset format,
2. implement an HLRP-owned temporal-window interface and weighted mixer,
3. keep the raw OXE loader only as a temporary comparison/fallback path,
4. do not invest in a permanent mixed raw-format pipeline.

## Plan refinement after design review

After re-checking the repo and LeRobot integration points, the migration plan was tightened in a few places to keep v1 simpler.

Changes to the plan:

- make image resizing explicit in the request/source contract,
- keep source output as resized `uint8` tensors,
- keep normalization in Stage 1 / Stage 2 adapters or downstream backends,
- remove `mix_block_size` from the initial design,
- remove worker-runtime eviction policy from the initial design,
- define validation as finite and deterministic per-source held-out episodes,
- keep source weights in mixer config, not in compiled metadata indices,
- treat action/state normalization as an adapter-stage concern, not a dataset-source concern,
- add quantitative parity checks instead of only qualitative comparison language.

Normalization decision after follow-up review:

- keep one global normalization object per training run,
- do not use per-dataset normalization,
- use raw canonical action/state tensors through the dataset interface,
- for single-source runs, use that source's `meta.stats`,
- for mixed runs, build one weighted merged stats object from the selected sources' `meta.stats`,
- weighting of merged stats should match the configured mixture, not only raw dataset sizes.

Test and benchmark planning decision:

- keep correctness and interface-contract checks in `tests/`,
- keep throughput measurement in `experiments/benchmarking/`,
- use synthetic pytest fixtures for most coverage,
- keep live LeRobot dataset smoke tests opt-in via environment flag,
- add one dedicated LeRobot-v3 dataloader benchmark script with batch-size / worker-count sweeps and JSON/CSV output.

Rationale:

- current OXE code already resizes in the dataset/adapter path,
- Stage 2 already does model-specific normalization downstream,
- Stage 2 and Stage 3 both already fit a single shared stats object better than source-dependent normalization,
- the repo already separates correctness tests and benchmarking scripts this way,
- locality knobs and eviction logic are optimization concerns and should only be added after profiling,
- explicit validation semantics and action normalization boundaries are required to make the migration implementable without hidden assumptions.

## Mixer survey

### LeRobot built-in multi-dataset path

Current vendored LeRobot code does contain `MultiLeRobotDataset`, but it is not a real weighted mixer and it is not wired into training.

Observed behavior:

- `lerobot/src/lerobot/datasets/lerobot_dataset.py`
  - `MultiLeRobotDataset` constructs one `LeRobotDataset` per repo id.
  - it concatenates them into one index space.
  - it keeps only the intersection of feature keys across all datasets.
  - it injects `dataset_index` into each item.
- `lerobot/src/lerobot/configs/default.py`
  - dataset config comments explicitly describe list-valued `repo_id` as concatenation.
- `lerobot/src/lerobot/configs/train.py`
  - `TrainPipelineConfig.validate()` raises `NotImplementedError` when `dataset.repo_id` is a list.
- `lerobot/src/lerobot/datasets/factory.py`
  - `make_dataset()` also raises `NotImplementedError` before the `MultiLeRobotDataset` branch can run.

Implication:

- built-in LeRobot multi-dataset support is currently concatenation-oriented and effectively disabled for training,
- it does not provide explicit source weighting,
- it is not sufficient for HLRP’s desired mixture control.

### External mixer patterns

#### OpenVLA

Source files:

- `https://github.com/openvla/openvla/blob/main/prismatic/vla/datasets/rlds/oxe/mixtures.py`
- `https://github.com/openvla/openvla/blob/main/prismatic/vla/datasets/rlds/dataset.py`

Observed pattern:

- define named mixtures as `(dataset_name, weight)` lists,
- compute dataset sizes once,
- optionally multiply weights by dataset sizes with `balance_weights`,
- repeat each source dataset indefinitely,
- apply trajectory transforms per source,
- flatten trajectories to frames,
- interleave the repeated frame streams with `DLataset.sample_from_datasets(datasets, sample_weights)`,
- shuffle after interleaving,
- allocate read/transform threads across datasets according to mixture weights.

Implication:

- OpenVLA mixes at the frame-stream level, not by concatenation,
- it treats weights as a first-class research parameter,
- it optimizes input pipeline resources around the configured mixture.

#### Octo

Source files:

- `https://github.com/octo-models/octo/blob/main/octo/data/oxe/oxe_dataset_mixes.py`
- `https://github.com/octo-models/octo/blob/main/octo/data/dataset.py`

Observed pattern:

- same overall structure as OpenVLA,
- named OXE mixtures are declared explicitly with per-dataset weights,
- `make_interleaved_dataset()` computes sizes, optionally balances weights by size, allocates threads by weights, repeats each source, flattens to frames, then uses `sample_from_datasets(...)`,
- applies frame transforms after interleaving,
- sets a small RAM budget on the iterator pipeline.

Implication:

- Octo and OpenVLA converge on the same design:
  - explicit named mixtures,
  - optional size balancing,
  - interleaving repeated per-dataset streams rather than simple concatenation.

#### Generic iterable mixing

Reference:

- `https://github.com/webdataset/webdataset/blob/main/src/webdataset/mix.py`

Observed pattern:

- `RoundRobin` and `RandomMix` provide iterable-level round-robin or probability-based source mixing,
- there is no robotics-specific handling for episodes, action windows, camera keys, or temporal validity.

Implication:

- generic data-loader libraries solve the source-selection part,
- robotics projects still need a domain-specific layer above that for temporal sampling and modality alignment.

#### Third-party LeRobot-specific weighted sampler

Reference:

- `https://pypi.org/project/robocandywrapper/`

Observed package behavior from `robocandywrapper==0.2.7`:

- package summary: "Sweet wrappers for extending and remixing LeRobot Datasets"
- ships `robocandywrapper/samplers/weighted.py`
- ships `robocandywrapper/samplers/factory.py`
- sampler config supports:
  - `dataset_weights`
  - `samples_per_epoch`
  - `shuffle`
  - `replacement`
  - optional episode selection metadata
- weighting semantics:
  - each dataset gets an effective size of `dataset_length * weight_multiplier`
  - per-dataset sample counts are computed once per epoch from the normalized effective sizes
  - flat indices are then drawn within each dataset range, concatenated, and globally shuffled

Important limitations:

- this is not upstream LeRobot,
- the package contents visible in the wheel are centered on `lerobot_21`, not LeRobot v3,
- the sampler expects a wrapper exposing flat dataset ranges and mixes at the concatenated-index level,
- it does not appear to handle request-dependent temporal validity, per-episode locality, or multi-camera temporal-window semantics.

Implication:

- yes, at least one third-party LeRobot-oriented weighted sampler exists,
- but it does not look like a mature LeRobot-v3-native solution that HLRP can adopt directly.

### HLRP-specific conclusion from the survey

There are two relevant precedents:

1. LeRobot’s built-in path is too weak for HLRP because it is concatenation-only and training-disabled.
2. OpenVLA and Octo both show that explicit weighted interleaving is the standard solution when mixing many robotics datasets.

For HLRP, the right design is still slightly different from both:

- keep the existing HLRP sampler idea of `dataset -> episode -> anchor`,
- keep explicit per-dataset weights,
- keep deterministic reshuffling/cycle behavior,
- do not rely on feature intersection as the normalization mechanism,
- return one HLRP-owned temporal-window sample type instead of raw backend items.

Why not copy OpenVLA/Octo literally:

- they operate in RLDS/TensorFlow pipelines and interleave repeated frame streams directly,
- HLRP needs request-dependent temporal validity for Stage 1 frame pairs and Stage 2 action chunks,
- HLRP already has a stronger sampler precedent in `openx_local_indexed_full.py` for weighted dataset/episode sampling.

So the external survey strengthens, not weakens, the current recommendation:

- keep the HLRP mixer owned by HLRP,
- make it LeRobot-v3-first,
- borrow explicit mixture registries and optional size-balancing concepts from OpenVLA/Octo,
- do not use `MultiLeRobotDataset` as the production solution.

## Artifacts added today

- plan: `docs/plan/lerobot_v3_dataset_unification_2026-03-03.md`
- probe script: `scripts/probe_lerobot_v3_dataset.py`

## Plan refinement

The plan document was expanded from a migration note into an implementation-grade design.

Main additions:

- explicit proposed module/function/class names for the new LeRobot-v3-first backend,
- canonical `DatasetRequest` / `DatasetSample` / `BatchedDatasetSample` / `SampleToken` interfaces,
- metadata-only compiled indexing plan,
- explicit training epoch definition for weighted mixed datasets,
- distributed-sampling semantics,
- cache ownership and persistence notes,
- RAM/CPU overhead estimates and expected bottlenecks.

Important new constraint captured in the plan:

- do not instantiate many filtered `LeRobotDataset(..., episodes=[...])` subsets for runtime mixing,
- keep one `LeRobotDataset` per source per worker,
- sample by absolute anchor index against a metadata-compiled episode index instead.
- after clarifying Stage 1 requirements, the plan now treats a dedicated `Stage1Batch` as the target consumer interface rather than preserving the legacy single-`frames` dict as the long-term shape.

## Temporal-query capability note

LeRobot itself already supports the retrieval side of more general Stage 1 requests:

- multiple camera keys,
- arbitrary temporal offsets via `delta_timestamps`,
- action windows,
- task text,
- per-key pad masks.

What is still limited today is not LeRobot's data format, but the current online-LAQ consumer code.

Current Stage 3 status:

- generic LeRobot data loading can return arbitrary temporal windows for observation keys,
- current `latent_smol` online-LAQ path in `lerobot_train.py` uses a single `laq_camera_key` and assumes exactly two frames,
- current HLRP Stage-3 policy code is partially generalized to multiple `laq_camera_keys`, but still rejects anything other than exactly one camera and exactly two observation steps when actually computing online latent targets.

Implication:

- the planned HLRP dataset contract should remain general over camera count and temporal length,
- but Stage 1 and Stage 3 LAQ-consumer code will need a separate model/adapter update to consume multi-camera or longer temporal sequences.

## Useful commands

Metadata-only probe:

```bash
python scripts/probe_lerobot_v3_dataset.py \
  --repo-id lerobot/jaco_play \
  --camera-key observation.images.image
```

Compare official vs mirror:

```bash
python scripts/probe_lerobot_v3_dataset.py \
  --repo-id lerobot/taco_play \
  --camera-key observation.images.rgb_static \
  --compare-repo-id FedorX8/taco_play_lerobot
```

Runtime Stage-2-style probe:

```bash
python scripts/probe_lerobot_v3_dataset.py \
  --repo-id lerobot/jaco_play \
  --camera-key observation.images.image \
  --probe-runtime \
  --episode 0 \
  --pair-offset-steps 5 \
  --action-chunk-size 10
```

## Implementation completed locally

Implemented the LeRobot-v3-first Stage 1/2 data path in the repo:

- `packages/common/lerobot_v3_types.py`
- `packages/common/lerobot_v3_stats.py`
- `packages/common/lerobot_v3_source.py`
- `packages/common/lerobot_v3_sampler.py`
- `packages/common/lerobot_v3_adapters.py`
- `packages/common/lerobot_v3_data.py`

Wired the new backend into:

- `packages/common/data_factory.py`
- `packages/laq/task.py`
- `packages/foundation/vla_backend_module.py`
- `scripts/2_train_laq.py`
- `scripts/4_train_foundation.py`

Added config + experiment entry points:

- `config/data/lerobot_v3.yaml`
- `config/experiment/stage1_laq_lerobot_v3_smoke.yaml`
- `config/experiment/stage2_smol_flow_lerobot_v3_smoke.yaml`

Added correctness coverage:

- unit/contract tests for request resolution, stats merging, index compilation, sampling, source runtime, collation, adapters, datamodule wiring
- live opt-in smoke test at `tests/test_lerobot_v3_smoke.py`
- Stage 1 / Stage 2 consumer regression coverage in `tests/test_vla_module_foundation_batch.py`

## Real-dataset choices

To keep local storage and download cost bounded, the live validation used small official v3 datasets:

- `lerobot/nyu_rot_dataset`
  - `440` frames, `14` episodes, one camera, action/state dim `7`
  - approx Hub file sum: `0.4 MB`
- `lerobot/cmu_franka_exploration_dataset`
  - `1990` frames, `199` episodes, two cameras
  - approx Hub file sum: `18.9 MB`
- `lerobot/asu_table_top`
  - `26113` frames, `110` episodes, one camera, action/state dim `7`
  - approx Hub file sum: `40.9 MB`

These were enough to validate:

- single-source Stage 1 path,
- single-source Stage 2 path,
- multi-camera retrieval,
- weighted two-source mixing.

## Bugs uncovered by live runs

Real local execution found four issues that synthetic tests did not expose initially:

1. LeRobot defaulted to `torchcodec`, but local `hlrp` had an incompatible TorchCodec runtime.
   Fix:
   - added explicit `video_backend` support to `LeRobotSingleSource`
   - set smoke/benchmark sources to `video_backend=pyav`

2. Mixed datasets with different state/action widths could not collate.
   Fix:
   - `collate_dataset_samples(...)` now pads `state` and `action` on the last dimension
   - `merge_weighted_stats(...)` now merges mismatched feature dimensions instead of assuming identical shapes

3. The new `FoundationBatch` Stage-2 path fed `uint8` video directly into the frozen LAQ encoder.
   Fix:
   - `_laq_video_from_foundation_batch(...)` now converts to `float32` in `[0,1]`

4. `validation_step(...)` still had one hard-coded OXE access path for state extraction.
   Fix:
   - `validation_step(...)` now handles `FoundationBatch` directly

All four now have regression coverage in tests.

## Smoke tests

Live pytest smoke:

- command:
  - `HLRP_RUN_LEROBOT_SMOKE=1 pytest -q tests/test_lerobot_v3_smoke.py`
- result:
  - `3 passed`

Covered by those smoke tests:

- native `Stage1Batch` on `lerobot/nyu_rot_dataset`
- `foundation` batch on `lerobot/nyu_rot_dataset`
- weighted mixed train batch on `lerobot/nyu_rot_dataset + lerobot/asu_table_top`
- multi-camera raw batch on `lerobot/cmu_franka_exploration_dataset`

## Local Stage 1 and Stage 2 runs

Stage 1 local smoke run:

- command:
  - `python scripts/2_train_laq.py experiment=stage1_laq_lerobot_v3_smoke logging.use_wandb=false`
- result:
  - completed successfully
  - output dir: `/mnt/data/workspace/runs_root/runs/2026-03-03_20-33-49_laq_lerobot_v3_smoke`
  - checkpoint: `/mnt/data/workspace/runs_root/runs/2026-03-03_20-33-49_laq_lerobot_v3_smoke/checkpoints/last.ckpt`

Stage 2 local smoke run:

- command:
  - `python scripts/4_train_foundation.py experiment=stage2_smol_flow_lerobot_v3_smoke model.laq.checkpoint=/mnt/data/workspace/runs_root/runs/2026-03-03_20-33-49_laq_lerobot_v3_smoke/checkpoints/last.ckpt logging.use_wandb=false`
- result:
  - completed successfully after fixing the two FoundationBatch bugs above
  - output dir: `/mnt/data/workspace/runs_root/runs/2026-03-03_20-37-13_stage2_smol_flow_lerobot_v3_smoke`
  - exported artifact:
    - `/mnt/data/workspace/runs_root/runs/2026-03-03_20-37-13_stage2_smol_flow_lerobot_v3_smoke/artifacts/smolvla_shared_stage2_artifact.pt`

## Benchmark results

Benchmark script added:

- `experiments/benchmarking/bench_lerobot_v3_dataloader.py`

Full sweep executed locally:

- output dir:
  - `experiments/benchmarking/results/lerobot_v3_full_sweep_2026-03-03`
- scenarios:
  - `stage1_single_nyu`
  - `stage1_mixed_nyu_asu`
  - `stage1_multicamera_cmu`
  - `stage2_nyu`
- grid:
  - batch sizes `8, 16, 32, 64`
  - workers `0, 2, 4, 8`
  - `64` runs total

Best observed throughput per scenario:

- `stage1_single_nyu`
  - best: `2917.2 samples/s` at `batch_size=16`, `num_workers=8`
- `stage1_mixed_nyu_asu`
  - best: `2467.2 samples/s` at `batch_size=16`, `num_workers=8`
- `stage1_multicamera_cmu`
  - best: `494.8 samples/s` at `batch_size=16`, `num_workers=8`
- `stage2_nyu`
  - best: `2997.5 samples/s` at `batch_size=8`, `num_workers=8`

Main benchmark takeaways:

- `num_workers=0` degrades sharply once batch size increases
  - e.g. `stage1_single_nyu` dropped to `42.3 samples/s` at `batch_size=64`
  - e.g. `stage2_nyu` dropped to `56.6 samples/s` at `batch_size=64`
- the weighted two-source mix is slower than the single-source case, but not catastrophically so
  - best mixed Stage 1: `2467.2 samples/s`
  - best single-source Stage 1: `2917.2 samples/s`
- multi-camera batches are meaningfully heavier
  - `stage1_multicamera_cmu` topped out at `494.8 samples/s`
  - payload roughly doubled versus the single-camera pair scenarios
- on this workstation, `8` workers gave the best throughput in all four benchmark scenarios

## Current status

Migration status after today:

- Phase 1 complete
- Phase 2 complete
- Phase 3 complete for explicit weighted mixing
- Phase 4 complete for Stage 1 + Stage 2 integration
- Phase 5 partially complete
  - Stage 1 smoke: done
  - Stage 2 smoke: done
  - benchmark: done
  - legacy OXE parity on matched anchors: still pending
