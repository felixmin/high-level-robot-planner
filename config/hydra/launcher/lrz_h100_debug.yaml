# LRZ H100 debug profile for short test runs
#
# Usage:
#   python scripts/2_train_laq.py -m hydra/launcher=lrz_h100_debug experiment=laq_oxe_debug

defaults:
  - lrz_h100

# Reduced resources for quick testing
gpus_per_node: 1
timeout_min: 30  # 30 minutes
mem_gb: 64
