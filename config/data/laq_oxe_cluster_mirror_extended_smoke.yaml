# @package data

# TF OXE preset: smoke test for the expanded cluster-mirror dataset list.

defaults:
  - dataset: oxe_cluster_mirror_extended_smoke
  - adapter: oxe_tf_minimal
  - loader: oxe_streaming
  - preprocess: pair_256
  - _self_

backend: oxe_tf

