# @package data

# TF OXE preset: language_table + bridge + rt1 + robonet, with the high-RAM TF adapter.

defaults:
  - dataset: oxe_all
  - adapter: oxe_tf_high_ram
  - loader: oxe_streaming
  - preprocess: pair_256
  - _self_

backend: oxe_tf

