# @package data.dataset

oxe:
  datasets:
    - name: robonet
      train_split: "train[:90%]"
      val_split: "train[90%:]"
      pair_offset_steps: 10
      weight: 1.0
      approx_num_pairs: 1494000

