# @package data

# TF OXE preset: language_table + bridge + rt1 + robonet.

defaults:
  - dataset: oxe_all
  - adapter: oxe_tf_low_ram
  - loader: oxe_streaming
  - preprocess: pair_256
  - _self_

backend: oxe_tf
