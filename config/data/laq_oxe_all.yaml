# @package data

# TF OXE preset: language_table + bridge + rt1 + robonet.

defaults:
  - dataset: oxe_all
  - adapter: oxe_tf_default
  - loader: oxe_streaming
  - preprocess: pair_256
  - _self_

backend: oxe_tf
