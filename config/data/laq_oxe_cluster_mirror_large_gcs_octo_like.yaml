# @package data

# TF OXE preset: 30+ dataset list + Octo-like skip-decoding + mix-before-decode.

defaults:
  - dataset: oxe_cluster_mirror_large
  - adapter: oxe_tf_gcs_octo_like
  - loader: oxe_streaming
  - preprocess: pair_256_no_meta
  - _self_

backend: oxe_tf

