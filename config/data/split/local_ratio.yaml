# @package data.split

# Train/val split behaviour for local-file datasets.

mode: ratio                  # ratio|metadata|fixed_count
seed: 42

val_ratio: 0.1
val_scene_filters: null
val_counts_per_dataset: null

