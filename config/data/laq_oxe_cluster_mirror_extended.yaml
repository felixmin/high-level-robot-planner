# @package data

# TF OXE preset: expanded dataset list matching the LRZ cluster mirror.

defaults:
  - dataset: oxe_cluster_mirror_extended
  - adapter: oxe_tf_default
  - loader: oxe_streaming
  - preprocess: pair_256
  - _self_

backend: oxe_tf

