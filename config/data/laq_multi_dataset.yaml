# @package data

# Local file-based multi-source preset (YouTube + Bridge).

defaults:
  - dataset: local_files_multi
  - loader: local_files
  - preprocess: pair_256
  - split: local_ratio
  - subset: full
  - _self_

backend: local_files
