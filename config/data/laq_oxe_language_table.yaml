# @package data

# TF OXE preset: language_table only.

defaults:
  - dataset: oxe_language_table_only
  - adapter: oxe_tf_default
  - loader: oxe_streaming
  - preprocess: pair_256
  - _self_

backend: oxe_tf
