# @package data

# TF OXE preset: 30+ dataset list + python "keep-hot" mixing for fast local training from GCS.

defaults:
  - dataset: oxe_cluster_mirror_large
  - adapter: oxe_tf_gcs_python_hot
  - loader: oxe_streaming
  - preprocess: pair_256_no_meta
  - _self_

backend: oxe_tf

