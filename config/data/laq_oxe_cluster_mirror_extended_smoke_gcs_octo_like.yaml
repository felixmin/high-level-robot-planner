# @package data

# Smoke-test preset: small dataset list + Octo-like skip-decoding + mix-before-decode.

defaults:
  - dataset: oxe_cluster_mirror_extended_smoke
  - adapter: oxe_tf_gcs_octo_like
  - loader: oxe_streaming
  - preprocess: pair_256_no_meta
  - _self_

backend: oxe_tf

