# @package data.adapter

# HuggingFace streaming OXE adapter (datasets streaming).
# These parameters are ONLY used by the HF backend (not by training/model code).

hf:
  train_shuffle_buffer: 100
  val_shuffle_buffer: 0
  samples_per_episode: 0   # 0 = yield all pairs in each episode

