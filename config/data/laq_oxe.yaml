# @package data

# TF OXE preset: oracle_sim (language_table_blocktorelative_oracle_sim).

defaults:
  - dataset: oxe_oracle_sim
  - adapter: oxe_tf_default
  - loader: oxe_streaming
  - preprocess: pair_256
  - _self_

backend: oxe_tf
