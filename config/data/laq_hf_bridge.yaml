# @package data

# HuggingFace OXE preset (no TensorFlow required).

defaults:
  - dataset: hf_bridge
  - adapter: oxe_hf_default
  - loader: oxe_streaming
  - preprocess: pair_256
  - _self_

backend: oxe_hf
