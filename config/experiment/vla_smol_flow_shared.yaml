# @package _global_

defaults:
  - /model@model: foundation_smol_flow_shared
  - /data@data: laq_oxe_all_high_ram
  - /training@training: vla_lightning
  - /cluster@cluster: local_dev
  - optional /user_config: local

experiment:
  name: vla_smol_flow_shared
  description: "SmolVLM shared trunk with flow latent-vector head + real-action head"

submit:
  script: 4_train_foundation

logging:
  tags: ["foundation", "smol", "flow", "multitask"]
