# @package _global_

defaults:
  - _stage3_rollout_base
  - /cluster@cluster: mcml_h100
  - /runtime: stage3_cluster
  - optional /user_config: local

experiment:
  name: stage3_rollout_cluster
  description: "Cluster Stage 3 rollout run (LeRobot eval + HLRP shared SmolVLA policy)"
