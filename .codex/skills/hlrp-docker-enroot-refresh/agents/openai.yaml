interface:
  display_name: "HLRP Docker Refresh"
  short_description: "Build, push, prune, import, and swap HLRP Docker/Enroot images."
  default_prompt: "Use this skill to run the HLRP local Docker build/push/prune workflow and import the resulting image to the LRZ cluster as an Enroot .sqsh, then swap it into place safely."
