# Unified Python requirements for a non-Docker local/conda environment.
#
# Install from the repo root after creating a Python 3.12 env and installing
# the torch stack separately for the target GPU, for example:
#   python -m pip install --index-url https://download.pytorch.org/whl/cu128 \
#     torch==2.10.0 torchvision==0.25.0 torchaudio==2.10.0
#   python -m pip install torchcodec==0.10.0
#   conda install -n <env> -c conda-forge ffmpeg -y
#
# LeRobot is installed from the local submodule, so its transitive dependencies
# come from lerobot/pyproject.toml instead of being duplicated here.
#
# Stage 1/2 code still lives under packages/, so stage 1/2 runs also need:
#   export PYTHONPATH=$PWD/packages:$PYTHONPATH

# Bundled local packages shared across all stages.
-e ./lerobot[libero,smolvla]
-e ./lerobot_policy_hlrp

# Shared HLRP runtime additions not provided by the LeRobot install.
hydra-core
omegaconf
wandb==0.24.0
matplotlib
rich
beartype
nvidia-ml-py

# Stage-1/2-only additions.
lightning
lightning-fabric
